<%= simple_form_for([@flat, @booking]) do |f| %>
  <%= f.input :start_booking, label: "Starting date", as: :date %>
  <%= f.input :end_booking, label: "Starting date", as: :date %>
  <%= f.submit class:"btn btn-primary" %>
<% end %>
